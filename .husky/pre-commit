#!/usr/bin/env sh

echo "🔄 正在检查暂存区代码是否符合标准... 🔄"

# 仅运行 lint-staged（它默认只检查 git 暂存区的文件）
# Windows 下文件过多时，限制参数长度并关闭并发，避免 eslint 进程被杀掉
pnpm run lint:staged && {
  echo "✅ 代码检查通过！🚀"
} || {
  echo "❌ 代码检查未通过！请修复错误后重新提交。❌"
  exit 1
}

exit 0
