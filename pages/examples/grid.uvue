<script setup lang="uts">
import TGrid from '@/components/TGrid'
import TGridItem from '@/components/TGridItem'
import TCard from '@/components/TCard'

/**
 * TGrid 组件示例页面
 */

const handleItemClick = (index: number): void => {
  uni.showToast({
    title: `点击了第 ${index + 1} 个格子`,
    icon: 'none'
  })
}
</script>

<template>
  <view class="demo-page">
    <!-- 基础用法 -->
    <TCard title="基础用法" padding="medium">
      <TGrid>
        <TGridItem icon="🏠" text="首页" />
        <TGridItem icon="📧" text="邮件" />
        <TGridItem icon="⭐" text="收藏" />
        <TGridItem icon="⚙️" text="设置" />
      </TGrid>
    </TCard>

    <!-- 自定义列数 -->
    <TCard title="自定义列数" padding="medium">
      <TGrid :columns="3">
        <TGridItem icon="🏠" text="首页" />
        <TGridItem icon="📧" text="邮件" />
        <TGridItem icon="⭐" text="收藏" />
        <TGridItem icon="⚙️" text="设置" />
        <TGridItem icon="📱" text="手机" />
        <TGridItem icon="💡" text="灯泡" />
      </TGrid>
    </TCard>

    <!-- 自定义间距 -->
    <TCard title="自定义间距" padding="medium">
      <TGrid :columns="4" :gap="10">
        <TGridItem icon="🏠" text="首页" />
        <TGridItem icon="📧" text="邮件" />
        <TGridItem icon="⭐" text="收藏" />
        <TGridItem icon="⚙️" text="设置" />
      </TGrid>
    </TCard>

    <!-- 显示边框 -->
    <TCard title="显示边框" padding="none">
      <TGrid :columns="4" :border="true">
        <TGridItem icon="🏠" text="首页" />
        <TGridItem icon="📧" text="邮件" />
        <TGridItem icon="⭐" text="收藏" />
        <TGridItem icon="⚙️" text="设置" />
        <TGridItem icon="📱" text="手机" />
        <TGridItem icon="💡" text="灯泡" />
        <TGridItem icon="🎨" text="画板" />
        <TGridItem icon="📷" text="相机" />
      </TGrid>
    </TCard>

    <!-- 正方形格子 -->
    <TCard title="正方形格子" padding="none">
      <TGrid :columns="4" :border="true" :square="true">
        <TGridItem icon="🏠" text="首页" />
        <TGridItem icon="📧" text="邮件" />
        <TGridItem icon="⭐" text="收藏" />
        <TGridItem icon="⚙️" text="设置" />
      </TGrid>
    </TCard>

    <!-- 可点击 -->
    <TCard title="可点击" padding="none">
      <TGrid :columns="3" :border="true" :clickable="true">
        <TGridItem icon="🏠" text="首页" @click="() => handleItemClick(0)" />
        <TGridItem icon="📧" text="邮件" @click="() => handleItemClick(1)" />
        <TGridItem icon="⭐" text="收藏" @click="() => handleItemClick(2)" />
        <TGridItem icon="⚙️" text="设置" @click="() => handleItemClick(3)" />
        <TGridItem icon="📱" text="手机" @click="() => handleItemClick(4)" />
        <TGridItem icon="💡" text="灯泡" @click="() => handleItemClick(5)" />
      </TGrid>
    </TCard>

    <!-- 自定义图标颜色 -->
    <TCard title="自定义图标颜色" padding="medium">
      <TGrid :columns="4">
        <TGridItem icon="🔴" text="红色" icon-color="#f5222d" />
        <TGridItem icon="🔵" text="蓝色" icon-color="#1890ff" />
        <TGridItem icon="🟢" text="绿色" icon-color="#52c41a" />
        <TGridItem icon="🟡" text="黄色" icon-color="#faad14" />
      </TGrid>
    </TCard>

    <!-- 徽标提示 -->
    <TCard title="徽标提示" padding="medium">
      <TGrid :columns="4">
        <TGridItem icon="🏠" text="首页" />
        <TGridItem icon="📧" text="邮件" badge="99+" />
        <TGridItem icon="⭐" text="收藏" :dot="true" />
        <TGridItem icon="⚙️" text="设置" badge="5" />
      </TGrid>
    </TCard>

    <!-- 使用图片 -->
    <TCard title="使用图片" padding="medium">
      <TGrid :columns="3">
        <TGridItem
          image="https://picsum.photos/100/100?random=1"
          text="图片1"
        />
        <TGridItem
          image="https://picsum.photos/100/100?random=2"
          text="图片2"
        />
        <TGridItem
          image="https://picsum.photos/100/100?random=3"
          text="图片3"
        />
      </TGrid>
    </TCard>

    <!-- 自定义内容 -->
    <TCard title="自定义内容" padding="none">
      <TGrid :columns="2" :border="true" :square="true">
        <TGridItem>
          <view class="custom-content">
            <text class="custom-title">自定义</text>
            <text class="custom-desc">完全自定义内容</text>
          </view>
        </TGridItem>
        <TGridItem icon="📋" text="普通格子" />
        <TGridItem>
          <view class="custom-content" style="background-color: #1890ff">
            <text class="custom-title" style="color: white">蓝色</text>
            <text class="custom-desc" style="color: white">背景色</text>
          </view>
        </TGridItem>
        <TGridItem icon="⚡" text="快速" />
      </TGrid>
    </TCard>
  </view>
</template>

<style lang="scss" scoped>
.demo-page {
  min-height: 100vh;
  background-color: #f5f5f5;
  padding: 20px;
}

.custom-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
  padding: 20px;
}

.custom-title {
  font-size: 16px;
  font-weight: bold;
  color: #323233;
  margin-bottom: 4px;
}

.custom-desc {
  font-size: 12px;
  color: #969799;
}
</style>
