<script setup lang="uts">
import { ref } from 'vue'

/**
 * TCollapse 折叠面板示例页面
 */

// 基础用法 - 多选模式
const activeNames = ref<string[]>(['1'])

// 手风琴模式
const accordionActive = ref<string>('1')

// 禁用状态
const disabledActive = ref<string[]>([])

// 自定义标题
const customActive = ref<string[]>(['1'])
</script>

<template>
  <view class="page">
    <view class="page-header">
      <text class="page-title">Collapse 折叠面板</text>
      <text class="page-desc">可以折叠/展开的内容区域</text>
    </view>

    <view class="page-content">
      <!-- 基础用法 -->
      <view class="section">
        <view class="section-title">
          <text class="section-title-text">基础用法</text>
        </view>
        <view class="section-content">
          <TCollapse v-model="activeNames">
            <TCollapseItem name="1" title="一致性 Consistency">
              <text class="item-content">
                与现实生活一致：与现实生活的流程、逻辑保持一致，遵循用户习惯的语言和概念。
              </text>
            </TCollapseItem>
            <TCollapseItem name="2" title="反馈 Feedback">
              <text class="item-content">
                控制反馈：通过界面样式和交互动效让用户可以清晰的感知自己的操作。
              </text>
            </TCollapseItem>
            <TCollapseItem name="3" title="效率 Efficiency">
              <text class="item-content">
                简化流程：设计简洁直观的操作流程，让用户快速完成目标。
              </text>
            </TCollapseItem>
          </TCollapse>
        </view>
      </view>

      <!-- 手风琴模式 -->
      <view class="section">
        <view class="section-title">
          <text class="section-title-text">手风琴模式</text>
        </view>
        <view class="section-content">
          <TCollapse v-model="accordionActive" :accordion="true">
            <TCollapseItem name="1" title="设计原则">
              <text class="item-content">
                一致、反馈、效率、可控是我们的设计原则。
              </text>
            </TCollapseItem>
            <TCollapseItem name="2" title="导航">
              <text class="item-content">
                导航可以解决用户在访问页面时，在哪里、去哪里、怎么去的问题。
              </text>
            </TCollapseItem>
            <TCollapseItem name="3" title="反馈">
              <text class="item-content">
                反馈是对用户行为的一种回应，给用户一个交代。
              </text>
            </TCollapseItem>
          </TCollapse>
        </view>
      </view>

      <!-- 禁用状态 -->
      <view class="section">
        <view class="section-title">
          <text class="section-title-text">禁用状态</text>
        </view>
        <view class="section-content">
          <TCollapse v-model="disabledActive">
            <TCollapseItem name="1" title="正常项">
              <text class="item-content">
                这是一个正常的折叠面板项，可以正常展开收起。
              </text>
            </TCollapseItem>
            <TCollapseItem name="2" title="禁用项" :disabled="true">
              <text class="item-content">
                这是一个禁用的折叠面板项，无法展开。
              </text>
            </TCollapseItem>
            <TCollapseItem name="3" title="正常项">
              <text class="item-content">
                这是另一个正常的折叠面板项。
              </text>
            </TCollapseItem>
          </TCollapse>
        </view>
      </view>

      <!-- 自定义标题 -->
      <view class="section">
        <view class="section-title">
          <text class="section-title-text">自定义标题</text>
        </view>
        <view class="section-content">
          <TCollapse v-model="customActive">
            <TCollapseItem name="1">
              <template #title>
                <view class="custom-title">
                  <text class="custom-title-icon">📱</text>
                  <text class="custom-title-text">移动端组件</text>
                </view>
              </template>
              <text class="item-content">
                专为移动端设计的轻量级 UI 组件库。
              </text>
            </TCollapseItem>
            <TCollapseItem name="2">
              <template #title>
                <view class="custom-title">
                  <text class="custom-title-icon">🎨</text>
                  <text class="custom-title-text">丰富的组件</text>
                </view>
              </template>
              <text class="item-content">
                提供丰富的基础组件，覆盖移动端各类场景。
              </text>
            </TCollapseItem>
            <TCollapseItem name="3">
              <template #title>
                <view class="custom-title">
                  <text class="custom-title-icon">⚡</text>
                  <text class="custom-title-text">高性能</text>
                </view>
              </template>
              <text class="item-content">
                性能优异，轻量级实现，保证流畅体验。
              </text>
            </TCollapseItem>
          </TCollapse>
        </view>
      </view>

      <!-- 无边框 -->
      <view class="section">
        <view class="section-title">
          <text class="section-title-text">无边框</text>
        </view>
        <view class="section-content">
          <TCollapse v-model="activeNames" :border="false">
            <TCollapseItem name="1" title="第一项">
              <text class="item-content">
                这是第一项的内容。
              </text>
            </TCollapseItem>
            <TCollapseItem name="2" title="第二项">
              <text class="item-content">
                这是第二项的内容。
              </text>
            </TCollapseItem>
          </TCollapse>
        </view>
      </view>

      <!-- 数据状态 -->
      <view class="section">
        <view class="section-title">
          <text class="section-title-text">当前状态</text>
        </view>
        <view class="section-content">
          <view class="state-box">
            <text class="state-label">基础用法展开项：</text>
            <text class="state-value">{{ activeNames.join(', ') || '无' }}</text>
          </view>
          <view class="state-box">
            <text class="state-label">手风琴展开项：</text>
            <text class="state-value">{{ accordionActive || '无' }}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</template>

<style lang="scss" scoped>
.page {
  min-height: 100vh;
  background-color: #f5f5f5;
  padding-bottom: 40px;

  &-header {
    background-color: #ffffff;
    padding: 20px;
    margin-bottom: 12px;
  }

  &-title {
    font-size: 20px;
    font-weight: bold;
    color: #303133;
    display: block;
    margin-bottom: 8px;
  }

  &-desc {
    font-size: 14px;
    color: #909399;
    display: block;
  }

  &-content {
    padding: 0;
  }
}

.section {
  margin-bottom: 12px;

  &-title {
    padding: 12px 20px;
    background-color: transparent;

    &-text {
      font-size: 14px;
      color: #909399;
    }
  }

  &-content {
    background-color: #ffffff;
  }
}

.item-content {
  display: block;
  font-size: 14px;
  color: #606266;
  line-height: 1.6;
}

.custom-title {
  display: flex;
  flex-direction: row;
  align-items: center;

  &-icon {
    font-size: 18px;
    margin-right: 8px;
  }

  &-text {
    font-size: 15px;
    font-weight: 500;
    color: #303133;
  }
}

.state-box {
  padding: 12px 20px;
  border-bottom: 1px solid #ebeef5;

  &:last-child {
    border-bottom: none;
  }
}

.state-label {
  font-size: 14px;
  color: #606266;
  margin-right: 8px;
}

.state-value {
  font-size: 14px;
  color: #409eff;
  font-weight: 500;
}
</style>
