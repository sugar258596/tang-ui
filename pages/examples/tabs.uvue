<script setup lang="uts">
import { ref } from 'vue'
import Tabs from '@/components/Tabs'
import TCard from '@/components/TCard'
import type { TabItem } from '@/components/Tabs/type'
import { useI18n } from '@/composables/useI18n.uts'

/**
 * 标签页组件示例页面
 * 展示 Tabs 组件的各种使用场景
 */

const { $t } = useI18n()

// 基础用法的标签数据
const basicTabs: TabItem[] = [
  { key: 'home', label: $t('examplePages.tabs.labels.home') },
  { key: 'profile', label: $t('examplePages.tabs.labels.profile') },
  { key: 'settings', label: $t('examplePages.tabs.labels.settings') },
  { key: 'about', label: $t('examplePages.tabs.labels.about') }
]
const activeTab1 = ref<string | number>('home')

// 卡片类型的标签数据
const cardTabs: TabItem[] = [
  { key: 'user', label: $t('examplePages.tabs.labels.user') },
  { key: 'order', label: $t('examplePages.tabs.labels.order') },
  { key: 'wallet', label: $t('examplePages.tabs.labels.wallet') }
]
const activeTab2 = ref<string | number>('user')

// 尺寸演示的标签数据
const sizeTabs: TabItem[] = [
  { key: 'tab1', label: $t('examplePages.tabs.labels.option1') },
  { key: 'tab2', label: $t('examplePages.tabs.labels.option2') },
  { key: 'tab3', label: $t('examplePages.tabs.labels.option3') }
]
const activeTab3 = ref<string | number>('tab1')

// 带图标和徽章的标签数据
const iconTabs: TabItem[] = [
  { key: 'message', label: $t('examplePages.tabs.labels.message'), badge: 5 },
  { key: 'notification', label: $t('examplePages.tabs.labels.notification'), badge: 99 },
  { key: 'todo', label: $t('examplePages.tabs.labels.todo'), badge: '新' }
]
const activeTab4 = ref<string | number>('message')

// 禁用状态的标签数据
const disabledTabs: TabItem[] = [
  { key: 'enabled1', label: $t('examplePages.tabs.labels.enabled1') },
  { key: 'disabled', label: $t('examplePages.tabs.labels.disabled'), disabled: true },
  { key: 'enabled2', label: $t('examplePages.tabs.labels.enabled2') }
]
const activeTab5 = ref<string | number>('enabled1')

// 底部标签栏
const bottomTabs: TabItem[] = [
  { key: 'home', label: $t('examplePages.tabs.labels.home') },
  { key: 'discover', label: $t('examplePages.tabs.labels.discover') },
  { key: 'me', label: $t('examplePages.tabs.labels.me') }
]
const activeTab6 = ref<string | number>('home')

// 自定义颜色
const colorTabs: TabItem[] = [
  { key: 'tab1', label: $t('examplePages.tabs.labels.option1') },
  { key: 'tab2', label: $t('examplePages.tabs.labels.option2') },
  { key: 'tab3', label: $t('examplePages.tabs.labels.option3') }
]
const activeTab7 = ref<string | number>('tab1')

/**
 * 标签切换事件处理
 */
const handleTabChange = (key: string | number): void => {
  console.log('切换到标签:', key)
  uni.showToast({
    title: `切换到: ${key}`,
    icon: 'none',
    duration: 1500
  })
}

/**
 * 标签点击事件处理
 */
const handleTabClick = (key: string | number, item: TabItem): void => {
  console.log('点击标签:', key, item)
}
</script>

<template>
  <scroll-view class="example-page" scroll-y>
    <!-- 页面头部 -->
    <view class="example-header">
      <text class="example-title">{{ $t('examplePages.tabs.title') }}</text>
      <text class="example-desc">{{ $t('examplePages.tabs.description') }}</text>
    </view>

    <!-- 基础用法 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.basic')" :description="$t('examplePages.tabs.sections.basic')">
        <Tabs
          :items="basicTabs"
          v-model="activeTab1"
          @change="handleTabChange"
        >
          <template #default="{ activeKey }">
            <view class="tab-content">
              <text class="content-text">{{ $t('examplePages.tabs.content.currentKey') }}: {{ activeKey }}</text>
              <view v-if="activeKey === 'home'" class="content-body">
                <text class="content-desc">{{ $t('examplePages.tabs.content.home') }}</text>
              </view>
              <view v-if="activeKey === 'profile'" class="content-body">
                <text class="content-desc">{{ $t('examplePages.tabs.content.profile') }}</text>
              </view>
              <view v-if="activeKey === 'settings'" class="content-body">
                <text class="content-desc">{{ $t('examplePages.tabs.content.settings') }}</text>
              </view>
              <view v-if="activeKey === 'about'" class="content-body">
                <text class="content-desc">{{ $t('examplePages.tabs.content.about') }}</text>
              </view>
            </view>
          </template>
        </Tabs>

        <text class="current-value">{{ $t('examplePages.tabs.content.currentSelected') }}: {{ activeTab1 }}</text>
      </TCard>
    </view>

    <!-- 卡片类型 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.card')" description="type='card'">
        <Tabs
          :items="cardTabs"
          v-model="activeTab2"
          type="card"
          @change="handleTabChange"
        >
          <template #default="{ activeKey }">
            <view class="tab-content card-content">
              <view v-if="activeKey === 'user'">
                <text class="content-title">{{ $t('examplePages.tabs.content.user') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.userPanel') }}</text>
              </view>
              <view v-if="activeKey === 'order'">
                <text class="content-title">{{ $t('examplePages.tabs.content.order') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.orderPanel') }}</text>
              </view>
              <view v-if="activeKey === 'wallet'">
                <text class="content-title">{{ $t('examplePages.tabs.content.wallet') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.walletPanel') }}</text>
              </view>
            </view>
          </template>
        </Tabs>
      </TCard>
    </view>

    <!-- 不同尺寸 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.size')" description="small、medium、large">
        <view class="size-demo">
          <text class="size-label">Small:</text>
          <Tabs :items="sizeTabs" v-model="activeTab3" size="small" type="card">
            <template #default="{ activeKey }">
              <view class="tab-content-mini">
                <text>{{ $t('examplePages.tabs.content.currentSelected') }}: {{ activeKey }}</text>
              </view>
            </template>
          </Tabs>
        </view>

        <view class="size-demo">
          <text class="size-label">Medium:</text>
          <Tabs :items="sizeTabs" v-model="activeTab3" size="medium" type="card">
            <template #default="{ activeKey }">
              <view class="tab-content-mini">
                <text>{{ $t('examplePages.tabs.content.currentSelected') }}: {{ activeKey }}</text>
              </view>
            </template>
          </Tabs>
        </view>

        <view class="size-demo">
          <text class="size-label">Large:</text>
          <Tabs :items="sizeTabs" v-model="activeTab3" size="large" type="card">
            <template #default="{ activeKey }">
              <view class="tab-content-mini">
                <text>{{ $t('examplePages.tabs.content.currentSelected') }}: {{ activeKey }}</text>
              </view>
            </template>
          </Tabs>
        </view>
      </TCard>
    </view>

    <!-- 带徽章 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.badge')" :description="$t('examplePages.tabs.sections.badge')">
        <Tabs
          :items="iconTabs"
          v-model="activeTab4"
          @change="handleTabChange"
          @tab-click="handleTabClick"
        >
          <template #default="{ activeKey }">
            <view class="tab-content">
              <view v-if="activeKey === 'message'">
                <text class="content-title">{{ $t('examplePages.tabs.content.messageCenter') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.unreadMessages') }}</text>
              </view>
              <view v-if="activeKey === 'notification'">
                <text class="content-title">{{ $t('examplePages.tabs.content.notifications') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.notificationCount') }}</text>
              </view>
              <view v-if="activeKey === 'todo'">
                <text class="content-title">{{ $t('examplePages.tabs.content.todoList') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.newTodo') }}</text>
              </view>
            </view>
          </template>
        </Tabs>
      </TCard>
    </view>

    <!-- 禁用状态 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.disabled')" :description="$t('examplePages.tabs.sections.disabled')">
        <Tabs
          :items="disabledTabs"
          v-model="activeTab5"
          @change="handleTabChange"
        >
          <template #default="{ activeKey }">
            <view class="tab-content">
              <text class="content-text">{{ $t('examplePages.tabs.content.currentSelected') }}: {{ activeKey }}</text>
              <text class="content-desc">{{ $t('examplePages.tabs.content.disabledDesc') }}</text>
            </view>
          </template>
        </Tabs>
      </TCard>
    </view>

    <!-- 底部标签栏 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.bottom')" description="tabPosition='bottom'">
        <Tabs
          :items="bottomTabs"
          v-model="activeTab6"
          tab-position="bottom"
          @change="handleTabChange"
        >
          <template #default="{ activeKey }">
            <view class="tab-content-bottom">
              <view v-if="activeKey === 'home'">
                <text class="content-title">{{ $t('examplePages.tabs.content.homeContent') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.bottomNavDesc') }}</text>
              </view>
              <view v-if="activeKey === 'discover'">
                <text class="content-title">{{ $t('examplePages.tabs.labels.discover') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.discoverContent') }}</text>
              </view>
              <view v-if="activeKey === 'me'">
                <text class="content-title">{{ $t('examplePages.tabs.labels.me') }}</text>
                <text class="content-text">{{ $t('examplePages.tabs.content.meContent') }}</text>
              </view>
            </view>
          </template>
        </Tabs>
      </TCard>
    </view>

    <!-- 自定义颜色 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.customColor')" :description="$t('examplePages.tabs.sections.customColor')">
        <Tabs
          :items="colorTabs"
          v-model="activeTab7"
          active-color="#ff6b6b"
          inactive-color="#999999"
          @change="handleTabChange"
        >
          <template #default="{ activeKey }">
            <view class="tab-content">
              <text class="content-text" style="color: #ff6b6b;">
                {{ $t('examplePages.tabs.content.customColorTheme') }}: {{ activeKey }}
              </text>
              <text class="content-desc">{{ $t('examplePages.tabs.content.redTheme') }}</text>
            </view>
          </template>
        </Tabs>
      </TCard>
    </view>

    <!-- 居中显示 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.centered')" description="centered">
        <Tabs
          :items="basicTabs"
          v-model="activeTab1"
          :centered="true"
          :scrollable="false"
        >
          <template #default="{ activeKey }">
            <view class="tab-content">
              <text class="content-text">{{ $t('examplePages.tabs.content.centered') }}: {{ activeKey }}</text>
            </view>
          </template>
        </Tabs>
      </TCard>
    </view>

    <!-- 无动画 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.noAnimation')" description="animated=false">
        <Tabs
          :items="basicTabs"
          v-model="activeTab1"
          :animated="false"
        >
          <template #default="{ activeKey }">
            <view class="tab-content">
              <text class="content-text">{{ $t('examplePages.tabs.content.noAnimation') }}: {{ activeKey }}</text>
            </view>
          </template>
        </Tabs>
      </TCard>
    </view>

    <!-- 使用说明 -->
    <view class="example-section">
      <TCard :title="$t('examplePages.tabs.sections.usage')">
        <view class="usage-info">
          <text class="info-title">{{ $t('examplePages.tabs.usage.props') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.vModel') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.type') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.size') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.tabPosition') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.scrollable') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.centered') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.activeColor') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.inactiveColor') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.animated') }}</text>

          <text class="info-title">{{ $t('examplePages.tabs.usage.events') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.change') }}</text>

          <text class="info-title">{{ $t('examplePages.tabs.usage.slots') }}</text>
          <text class="info-item">{{ $t('examplePages.tabs.usage.default') }}</text>
        </view>
      </TCard>
    </view>
  </scroll-view>
</template>

<style lang="scss" scoped>
.example-page {
  padding: 32rpx;
  background-color: #f5f7fa;
  min-height: 100vh;
}

.example-header {
  margin-bottom: 32rpx;
}

.example-title {
  font-size: 48rpx;
  font-weight: bold;
  color: #303133;
  margin-bottom: 16rpx;
  display: block;
}

.example-desc {
  font-size: 28rpx;
  color: #909399;
  display: block;
}

.example-section {
  margin-bottom: 32rpx;
}

// 标签内容样式
.tab-content {
  padding: 40rpx;
  background-color: #fff;
  min-height: 200rpx;

  .content-text {
    font-size: 32rpx;
    color: #303133;
    display: block;
    margin-bottom: 16rpx;
  }

  .content-desc {
    font-size: 28rpx;
    color: #909399;
    display: block;
  }

  .content-body {
    margin-top: 16rpx;
    padding: 20rpx;
    background-color: #f5f7fa;
    border-radius: 8rpx;
  }

  .content-title {
    font-size: 36rpx;
    font-weight: bold;
    color: #303133;
    display: block;
    margin-bottom: 20rpx;
  }
}

.card-content {
  border-radius: 0 0 12rpx 12rpx;
}

.tab-content-mini {
  padding: 20rpx;
  background-color: #fff;
  min-height: 100rpx;
  display: flex;
  align-items: center;
  justify-content: center;

  text {
    font-size: 28rpx;
    color: #606266;
  }
}

.tab-content-bottom {
  padding: 40rpx;
  background-color: #fff;
  min-height: 300rpx;

  .content-title {
    font-size: 36rpx;
    font-weight: bold;
    color: #303133;
    display: block;
    margin-bottom: 20rpx;
  }

  .content-text {
    font-size: 28rpx;
    color: #909399;
    display: block;
  }
}

.current-value {
  display: block;
  margin-top: 20rpx;
  padding: 16rpx;
  background-color: #ecf5ff;
  color: #409eff;
  font-size: 28rpx;
  border-radius: 8rpx;
}

// 尺寸演示
.size-demo {
  margin-bottom: 24rpx;

  &:last-child {
    margin-bottom: 0;
  }

  .size-label {
    display: block;
    font-size: 28rpx;
    color: #606266;
    margin-bottom: 12rpx;
    font-weight: 500;
  }
}

// 使用说明
.usage-info {
  padding: 20rpx;

  .info-title {
    display: block;
    font-size: 32rpx;
    font-weight: bold;
    color: #303133;
    margin-top: 24rpx;
    margin-bottom: 16rpx;

    &:first-child {
      margin-top: 0;
    }
  }

  .info-item {
    display: block;
    font-size: 28rpx;
    color: #606266;
    line-height: 48rpx;
    padding-left: 20rpx;
  }
}
</style>